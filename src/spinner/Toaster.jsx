import React from 'react';
import { ToastContainer } from 'react-toastify';
import '../../node_modules/react-toastify/dist/ReactToastify.css';

const Toaster = (loading) => {
  return <ToastContainer />;
};

export default Toaster;
